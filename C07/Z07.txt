#include <stdio.h>
#include <stdlib.h>

void wytnij (char *tab,int n,int m){
int i,dl;
for (dl=0;tab[dl]!=0;dl++);
if dl+1>m)
    {
    for (i=0;i+m<dl;i++)
    nap[n+i]=nap[i+m+1];
    }
else
    {
    if ((n<dl)&&dl+1<=m))
        {
        tab[n]=0;
        }
    }
}
